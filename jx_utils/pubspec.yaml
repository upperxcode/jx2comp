name: jx_utils
description: "A new Flutter package project."
version: 0.0.1


environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  indexed_db: ^1.0.1
  path_provider: ^2.1.5
  share_plus: ^12.0.0
  stack_trace: ^1.12.1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

