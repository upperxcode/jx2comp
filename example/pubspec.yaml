name: example
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter
  jx_data:
    path: ../jx_data
  jx2_widgets:
    path: ../jx2_widgets
  jx2_grid:
    path: ../jx2_grid
  jx_utils:
    path: ../jx_utils
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/themes/custom.json
    - assets/icons/
    - assets/images/
